<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Some LiveATC stream mounts are still served over plain HTTP. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">liveatc.net</domain>
    </domain-config>
</network-security-config>
